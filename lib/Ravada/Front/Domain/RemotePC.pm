package Ravada::Front::Domain::RemotePC;

use Carp qw(confess);
use Data::Dumper;
use Moose;

extends 'Ravada::Front::Domain';

no warnings "experimental::signatures";
use feature qw(signatures);

1;
