    <div class="alert alert-primary" ng-show="showmachine.needs_restart && !showmachine.requests">
        <div class="panel-heading">
            <%=l 'The changes will apply on next restart' %>
            <button
                ng-click="shutdown_start()"
            ><%=l 'reboot now' %></button>
        </div>
    </div>

